import { Helmet } from "react-helmet-async"; import { useParams, Link } from "react-router-dom"; import { squadRoles } from "../data/squadRoles"; import ShareButtons from "../components/ShareButtons"; import Ad from "../components/Ad";  const METRIC_KEYS = ["reliability", "funFactor", "dramaLevel", "loyalty", "cohesion"] as const; const METRIC_LABELS: Record<string, string> = {   reliability: "Reliability",   funFactor: "Fun Factor",   dramaLevel: "Drama Level",   loyalty: "Loyalty",   cohesion: "Cohesion", }; const METRIC_COLORS: Record<string, string> = {   reliability: "#1E40AF",   funFactor: "#0D9488",   dramaLevel: "#DC2626",   loyalty: "#16A34A",   cohesion: "#EAB308", };  export default function RoleDetailPage() {   const { slug } = useParams<{ slug: string }>();   const role = squadRoles.find((r) => r.slug === slug);   if (!role) return <div style={{ minHeight: '60vh', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 16 }}><Helmet><meta name="robots" content="noindex,nofollow" /><title>404 - Not Found | Squad Audit</title></Helmet><h1 style={{ fontSize: 48, fontWeight: 800 }}>404</h1><p>Page not found</p><Link to="/" style={{ textDecoration: 'none', fontWeight: 600 }}>Go Home</Link></div>;    const url = `https://squad.csskey.com/roles/${role.slug}`;   const compatible = squadRoles.filter((r) => role.compatibility.includes(r.slug));   const related = squadRoles.filter((r) => r.slug !== role.slug && !role.compatibility.includes(r.slug)).slice(0, 3);    return (     <div className="fade-in" style={{ maxWidth: 680, margin: "0 auto" }}>       <Helmet>         <title>{role.name} - Squad Role Guide | Squad Audit</title>         <meta name="description" content={`${role.name}: ${role.tagline} Responsibilities, strengths, weaknesses, and health metrics.`} />         <meta property="og:title" content={`${role.name} 鈥擲quad Role`} />         <meta property="og:description" content={role.tagline} />         <meta property="og:url" content={url} />         <meta property="og:type" content="article" />         <script type="application/ld+json">           {JSON.stringify({             "@context": "https://schema.org",             "@type": "Article",             headline: `${role.name} 鈥擲quad Role`,             description: role.tagline,             url,             author: { "@type": "Person", name: "Marcus Chen" },             publisher: { "@type": "Organization", name: "Squad Audit" },             mainEntityOfPage: url,           })}         </script>         <script type="application/ld+json">{JSON.stringify({           "@context": "https://schema.org",           "@type": "BreadcrumbList",           "itemListElement": [             { "@type": "ListItem", position: 1, name: "Home", item: "https://squad.csskey.com/" },             { "@type": "ListItem", position: 2, name: "Roles", item: "https://squad.csskey.com/roles" },             { "@type": "ListItem", position: 3, name: role.name, item: url }           ]         })}</script>       </Helmet>        <div style={{ marginBottom: 24 }}>         <Link to="/roles" style={{ color: "var(--text-faint)", fontSize: 12, fontFamily: "var(--font-mono)" }}>鈫扐ll Roles</Link>       </div>        <div style={{ textAlign: "center", marginBottom: 40 }}>         <div style={{ fontSize: "4rem", marginBottom: 16 }}>{role.emoji}</div>         <h1 className="gradient-text" style={{ fontFamily: "var(--font-heading)", fontSize: "clamp(2rem, 5vw, 2.75rem)", fontWeight: 700, marginBottom: 8 }}>{role.name}</h1>         <p style={{ fontSize: 16, color: "var(--primary)", fontStyle: "italic", fontFamily: "var(--font-body)" }}>{role.tagline}</p>       </div>        {/* Audit scores */}       <div className="audit-card" style={{ padding: "28px 32px", marginBottom: 24 }}>         <div className="eyebrow" style={{ marginBottom: 16 }}>Health Metrics</div>         {METRIC_KEYS.map((key) => (           <div key={key} style={{ marginBottom: 16 }}>             <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 6 }}>               <span style={{ fontSize: 13, color: "var(--text-soft)", fontFamily: "var(--font-mono)" }}>{METRIC_LABELS[key]}</span>               <span style={{ fontSize: 13, color: METRIC_COLORS[key], fontWeight: 700, fontFamily: "var(--font-mono)" }}>{role.auditScore[key]}/100</span>             </div>             <div className="metric-bar">               <div className="metric-bar-fill" style={{ width: `${role.auditScore[key]}%`, background: METRIC_COLORS[key] }} />             </div>           </div>         ))}       </div>        {/* Description */}       <div className="audit-card" style={{ padding: "28px 32px", marginBottom: 24 }}>         <div className="eyebrow" style={{ marginBottom: 12 }}>Role Analysis</div>         <p className="dropcap" style={{ fontSize: 16, lineHeight: 1.8, color: "var(--text)", fontFamily: "var(--font-body)", textAlign: "justify" }}>{role.description}</p>       </div>        {/* Responsibilities */}       <div className="audit-card" style={{ padding: "28px 32px", marginBottom: 24 }}>         <div className="eyebrow" style={{ marginBottom: 12 }}>Responsibilities</div>         <ul style={{ listStyle: "none", padding: 0, margin: 0 }}>           {role.responsibilities.map((r, i) => (             <li key={i} style={{ fontSize: 15, color: "var(--text)", padding: "8px 0", borderBottom: i < role.responsibilities.length - 1 ? "1px solid var(--border)" : "none", fontFamily: "var(--font-body)" }}>               <span style={{ color: "var(--secondary)", marginRight: 12 }}>鈻?/span>{r}             </li>           ))}         </ul>       </div>        {/* Strengths & Weaknesses */}       <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 24 }}>         <div className="audit-card" style={{ padding: "24px 28px" }}>           <div className="eyebrow" style={{ marginBottom: 8, color: "var(--success)" }}>Strengths</div>           {role.strengths.map((s, i) => (             <div key={i} style={{ fontSize: 14, color: "var(--text)", padding: "4px 0", fontFamily: "var(--font-body)" }}>               <span style={{ color: "var(--success)", marginRight: 8 }}>+</span>{s}             </div>           ))}         </div>         <div className="audit-card" style={{ padding: "24px 28px" }}>           <div className="eyebrow" style={{ marginBottom: 8, color: "var(--danger)" }}>Weaknesses</div>           {role.weaknesses.map((w, i) => (             <div key={i} style={{ fontSize: 14, color: "var(--text)", padding: "4px 0", fontFamily: "var(--font-body)" }}>               <span style={{ color: "var(--danger)", marginRight: 8 }}>鈭?/span>{w}             </div>           ))}         </div>       </div>        <Ad />        {/* Compatible roles */}       {compatible.length > 0 && (         <section style={{ marginBottom: 32 }}>           <div className="eyebrow" style={{ marginBottom: 12 }}>Most Compatible With</div>           <div style={{ display: "flex", gap: 12, flexWrap: "wrap" }}>             {compatible.map((c) => (               <Link key={c.slug} to={`/roles/${c.slug}`} style={{ textDecoration: "none" }}>                 <div className="audit-card" style={{ padding: "12px 20px", display: "flex", alignItems: "center", gap: 8 }}>                   <span style={{ fontSize: "1.5rem" }}>{c.emoji}</span>                   <span style={{ fontSize: 14, color: "var(--text)", fontFamily: "var(--font-body)" }}>{c.name}</span>                 </div>               </Link>             ))}           </div>         </section>       )}        {/* Related roles */}       {related.length > 0 && (         <section style={{ marginBottom: 32 }}>           <div className="eyebrow" style={{ marginBottom: 12 }}>Other Squad Roles</div>           <div style={{ display: "flex", gap: 12, flexWrap: "wrap" }}>             {related.map((c) => (               <Link key={c.slug} to={`/roles/${c.slug}`} style={{ textDecoration: "none" }}>                 <div className="audit-card" style={{ padding: "12px 20px", display: "flex", alignItems: "center", gap: 8 }}>                   <span style={{ fontSize: "1.5rem" }}>{c.emoji}</span>                   <span style={{ fontSize: 14, color: "var(--text)", fontFamily: "var(--font-body)" }}>{c.name}</span>                 </div>               </Link>             ))}           </div>         </section>       )}        <div style={{ marginTop: 32 }}>         <ShareButtons url={url} title={`${role.name} 鈥?{role.tagline}`} />       </div>     </div>   ); }